**Figure (oscilloscope screenshot — single-trace waveform on a graticule, p11).** Monochrome (black trace on white), cropped to the graticule area only.

Graticule: a solid black rectangular border enclosing a dotted grid of **10 horizontal divisions × 8 vertical divisions** (9 interior vertical dotted lines, 7 interior horizontal dotted lines, evenly spaced). Every division is subdivided by small dot ticks (5 sub-ticks per division). **No text of any kind appears in the image** — no channel labels, no volts/div or time/div readout, no measurement annotations, no cursor values, no axis numbers. All levels below are therefore given in graticule divisions read off the grid, not in absolute units.

Waveform — a single positive rectangular pulse:

- Starts at the left edge at a flat low level lying exactly on the horizontal graticule line **2 divisions up from the bottom** (6 divisions below the top).
- Rises with a near-vertical edge at the **first vertical graticule line, 1.0 division in from the left edge**.
- Overshoots at the top of the rising edge to roughly **0.6 division above** the settled high level, followed by a short damped ring of two or three cycles that decays within about 0.15 division of horizontal travel.
- Settles to a flat high level lying exactly on the horizontal graticule line **2 divisions down from the top**. Pulse amplitude is therefore **4.0 divisions** peak-to-peak between the two flat levels.
- The high level stays flat (no droop or tilt visible) until the falling edge.
- Falls with a near-vertical edge at approximately **7.25 divisions in from the left edge** (about a quarter of a division past the seventh vertical graticule line). Measured between the two edges, the pulse width is about **6.25 divisions**.
- Undershoots at the bottom of the falling edge to roughly **0.75 division below** the low level, with a short damped ring (visibly larger and slightly longer-lived than the rising-edge ring).
- Returns to the same flat low level (2 divisions up from the bottom) and runs flat to the right edge of the graticule.

Only one trace is present; no second channel, no cursors, no trigger marker and no reference-level markers are drawn.
