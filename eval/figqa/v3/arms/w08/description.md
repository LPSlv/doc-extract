**Figure (2-D region / partition map, five solid colour-filled regions).** A
wide rectangular plot whose interior is tiled without gaps by five polygonal
regions, each a flat solid colour with a black bold text label inside it. No
title, no legend, no gridlines; a thin black rectangular frame bounds the plot.

Axes. X: **`φ(w)`** (phi of w), labelled ticks at **0.4, 0.5, 0.6, 0.7, 0.8,
0.9, 1, 1.1, 1.2, 1.3, 1.4**; the plotted range runs from about **0.34** at the
left spine (unlabelled) to **1.4** at the right spine. Y: **"function"**,
labelled ticks at **0, 0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5**;
range **0 to 0.5**.

Regions, with their labels, fill colours and approximate boundaries (vertex
coordinates read from the axes):

- **R4** — purple, upper-left. Bounded left by the plot's left edge (x ≈ 0.34)
  from y ≈ 0.21 up to y = 0.5, above by the top edge from x ≈ 0.34 to
  x ≈ 0.79, and below-right by a straight line running from about
  **(0.42, 0.135)** up to **(0.79, 0.50)**. Label "R4" sits near
  (0.50, 0.42).
- **R3** — red, by far the largest region, filling the centre and right of the
  plot. Bounded above-left by the R4 diagonal (0.42, 0.135) → (0.79, 0.50),
  above by the top edge from x ≈ 0.79 to x = 1.4, right by the right edge from
  y = 0.5 down to y ≈ 0.11, below-right by the R2 diagonal
  (1.14, 0) → (1.40, ~0.11), below by the x-axis from x ≈ 0.56 to x ≈ 1.14,
  and below-left by the R5 diagonal (0.42, 0.135) → (0.56, 0). Label "R3" sits
  near (0.83, 0.38).
- **R1** — blue, a small triangle against the left edge. Vertices approximately
  **(0.34, 0.20)**, **(0.34, 0.06)** and an apex at **(0.42, 0.135)**, wedged
  between R4 above and R5 below. Label "R1" sits near (0.37, 0.145).
- **R5** — yellow/amber, lower-left triangle. Vertices approximately
  **(0.34, 0.06)** (left edge, below R1), **(0.34, 0)** (bottom-left corner),
  **(0.56, 0)** on the x-axis, with apex at **(0.42, 0.135)**. Label "R5" sits
  near (0.44, 0.05).
- **R2** — green, lower-right triangle. Vertices approximately **(1.14, 0)**,
  **(1.40, 0)** (bottom-right corner) and **(1.40, 0.11)** on the right edge.
  Label "R2" sits near (1.31, 0.05).

All four internal boundaries are straight lines. Regions R1, R4 and R5 meet at
the single point ≈ (0.42, 0.135), which is also the leftmost point of R3. No
numeric values are printed inside the regions beyond the R1–R5 names, and no
colour key is given.
