**Figure (multi-panel composite: gene-targeting schematic + Southern blots + PCR gels + flow-cytometry histograms and dot plots, p2).** Five labelled panels, A–E. No overall title or caption text is inside the image.

**Panel A — targeting-strategy schematic (line-and-box gene maps, four rows numbered (1)–(4)).**
A scale bar at the top right of the panel is labelled `1 kb`.

Row (1), wild-type locus: a horizontal line carrying exon boxes. Leftmost is a large box whose left half is white (unfilled) and right half is black; a downward arrow labelled `ATG` points to it. To its right, along the line, come two separate black boxes, then a cluster of three narrow black boxes close together. Further right a vertical tick labelled `B` marks a site, and just to its right a short red bar labelled `5' probe` sits under the line. Next is a large terminal box whose left portion is black and right portion is white; a downward arrow labelled `STOP` points to the black/white junction. A second vertical tick labelled `B` sits to the right on the line. A horizontal bracket spanning the two `B` ticks is labelled `8kb`.

Row (2), targeting construct (drawn enlarged, connected to row (1) by dashed lines): a black box abutting a white box containing the green text `GFP`; then a green right-pointing triangle; then a blue box containing white text `Neo`, with a vertical tick labelled `B` above it; then a second green right-pointing triangle; then a white box; line continues right.

Row (3), targeted (knock-in) allele: the full locus with the same exon boxes as row (1) at left (large white/black box, two black boxes, cluster of three narrow black boxes), then a vertical tick labelled `B`, then a red bar labelled `5' probe`, then the inserted cassette — black box + white box `GFP` (green text), green triangle, blue `Neo` box with a `B` tick above it, green triangle, white box — then the line continues right to a further vertical tick labelled `B`. A red bar labelled `Neo probe` sits below the `Neo` box. Two horizontal brackets give fragment sizes: `6.3kb` from the left `B` to the `B` above `Neo`, and `6.6kb` from the `B` above `Neo` to the right-hand `B`.

Row (4), post-excision allele: the locus with the same exon boxes, an unlabelled vertical tick, then a black box + white `GFP` box, a single green triangle, then a white box; line continues right. Dashed lines lead from row (3) to row (4) with the annotation `Autoexcision in males` at the right.

Panel A legend (bottom of panel), three items: green right-pointing triangle = `LoxP sites`; blue box containing `Neo` = `Selection cassette`; `B: BAMHI sites`.

**Panel B — two Southern blots (greyscale autoradiographs).**
Left blot titled `5' Probe`; lane headings left to right: `1`, `2`, `3`, `JM8`. A size ladder is printed to the left of the left blot with tick marks at `10kb`, `8`, `6`, `5`, `4`. Two band rows are annotated at the right edge: `WT` (band running just under the 8 mark, present in all four lanes; the `JM8` band is the darkest/sharpest) and `KI` (band running just above the 6 mark, present in lanes 1, 2 and 3 and absent from `JM8`).
Right blot titled `Neo Probe`; lane headings `1`, `2`, `3`, `JM8`. A single band row is annotated `KI` at the right, present in lanes 1, 2 and 3 and essentially absent (at most a trace) in `JM8`. No size ladder is printed alongside the right blot. No other bands are visible on either blot.

**Panel C — two agarose gel photographs (white bands on black), stacked.**
Top gel: column headings above the gel read `+/+` and `+/GFP`. A DNA-ladder lane occupies the left of the gel; ladder annotations at the left read `0.5kb` and `0.1kb`. A single bright band, positioned below the 0.5kb mark and well above the 0.1kb mark, is present in both the `+/+` and `+/GFP` lanes. Right-hand labels, stacked: `PCR`, `WT`, `Allele`.
Bottom gel: column headings `+/+` and `+/GFP`; ladder lane at left with annotations `0.5kb` and `0.1kb`. A single bright band, at roughly the same height as in the top gel, is present only in the `+/GFP` lane; the `+/+` lane is empty. Right-hand labels, stacked: `PCR`, `GFP`, `Allele`.

**Panel D — two overlaid flow-cytometry histograms (grey filled background histogram plus coloured outline traces), with a horizontal gate bar drawn on the right-hand shoulder of each.**
Left histogram, x-axis caption `Eomes`. In-plot gate values, colour-coded: `het: 9` (blue), `hom: 9.5` (red), `wt: 8.5` (green). Text below the plot: `het MFI:1800` (blue), `hom MFI:2100` (red), `wt MFI:2400` (green).
Right histogram, x-axis caption `GFP`. In-plot gate values: `het: 8.5` (blue), `hom: 9.5` (red); no green/wt trace or value is shown in this plot. Text below the plot: `het MFI:1225` (blue), `hom MFI:2000` (red).
Neither histogram carries numeric axis ticks or a y-axis label; the x-axes show unlabelled minor tick marks only. All three traces overlap closely at the main peak in the left plot; the two traces overlap closely in the right plot.

**Panel E — two flow-cytometry density (pseudocolour) dot plots side by side.**
Plot titles: `C57BL/6` (left) and `Eomes GFP/+` (right). Shared y-axis caption `Eomes-GFP`, with the single tick label `10⁰` at the axis origin. Shared x-axis caption `intracellular Eomes`, with the single tick label `10⁰` at each plot's origin. No upper tick labels or decade marks are legible.
Left plot (`C57BL/6`): a single elongated population sitting low on the Eomes-GFP axis, spread horizontally across intermediate intracellular-Eomes values, with a dense red/green core right of centre and blue scatter around it; no upward extension.
Right plot (`Eomes GFP/+`): a larger population in the same horizontal position but extending upward on the Eomes-GFP axis, with a broad dense red/green core and a diagonal blue tail running up and to the right.
