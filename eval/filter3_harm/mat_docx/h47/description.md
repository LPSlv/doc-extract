**Masthead (top of page).** Vishay logo, www.vishay.com; device `VSMA1094750`;
`Vishay Semiconductors`.

**Figure group 1 — `RECOMMENDED FOOTPRINT`** (three mechanical plan views in a
row plus one component-location view, all dimensions in millimetres, unlabelled
units).

*View 1 (left) — copper.* Plan view of the land pattern: two large square-ish
pads, one above the other, each flanked left and right by a narrow vertical
tab, giving an H-shaped outline overall. Horizontal and vertical dash-dot
centrelines cross at the middle. Dimensions: `5.5` across the top (overall
width), `3.5` (inner width, between the tab inner edges), `5.5` down the left
side (overall height), `2.8` across the bottom (pad width). Key below the view:
an empty white square swatch labelled `Cu area`.

*View 2 (centre) — solder resist.* Square field filled with diagonal hatching
(the solder resist), with three horizontal openings stacked vertically in the
middle (the middle one narrower, drawn as a solid bar between two outlined
bars). Dimensions: `2.8` on the left side (vertical, height of the opening
group), `2.8` across the bottom (width), `0.55` at the right (vertical, opening
height). Key: hatched swatch labelled `Solder resist`.

*View 3 (right) — solder stencil.* Two hatched rectangular stencil apertures,
upper and lower, separated by a narrow hatched bar, between two vertical side
tabs. Dimensions: `2.6` on the left side (vertical overall), `0.4` (small
vertical dimension at the left of the gap), `0.9` at the right (vertical,
aperture height), `2.6` across the bottom (width). Key: hatched swatch labelled
`Solder stencil`.

*View 4 (below view 1) — component location.* Same H-shaped pad outline with a
large circle (the LED emitter body) drawn centred on the pads, plus a small
circle at its lower left with a leader labelled `Cathode marking`. Caption
beneath: `Component location on pad`.

Drawing identification printed to the right of the footprint group:
`Drawing-No.: 6.550-5366.9-3`, `Issue: 2; 23.02.2023`.

**Figure — `Fig. 11 - Lead (Pb)-free (Sn) Infrared Reflow Solder Profile
According to J-STD-020E for Surface-Mount Components`** (line chart, under the
heading `SOLDER PROFILE`).

- X axis: `Time (s)`, 0 to 300, labelled ticks every 50 (0, 50, 100, 150, 200,
  250, 300).
- Y axis: `Temperature (°C)`, 0 to 300, labelled ticks every 50 (0, 50, 100,
  150, 200, 250, 300).
- Three horizontal dashed reference lines with left-hand labels `255 °C`,
  `240 °C` and `217 °C`.
- Two nearly coincident traces (one blue, one orange) describing the reflow
  profile: rising from about 25 °C at t = 0 to about 150 °C at t ≈ 50 s; a
  gentler soak ramp from ~150 °C to ~200 °C between about 50 s and 170 s; a
  climb to a rounded peak of about 250–260 °C between roughly 200 s and 245 s
  (the orange trace peaks slightly above the blue); then a steep cool-down back
  to about 25 °C by t ≈ 290 s (all read from axis).
- In-plot annotations: `Max. 260 °C` (arrow to the peak), `245 °C` (boxed, at
  the peak region), `Max. 30 s` (double-headed arrow across the peak),
  `Max. 120 s` (double-headed arrow across the soak region, roughly 40–160 s),
  `Max. 100 s` (double-headed arrow across the ramp-to-peak region, roughly
  165–265 s), `Max. ramp up 3 °C/s` (on the initial rise),
  `Max. ramp down 6 °C/s` (on the cool-down), `Max. 2 cycles allowed`
  (lower right of the plot area).
- Drawing/figure id printed at the lower left of the plot: `23192`.

The DRYPACK / FLOOR LIFE / DRYING text in the right-hand column of the page is
ordinary extractable prose and is not reproduced here.

Page footer: "Rev. 1.0, 13-Nov-2023", page 7, "Document Number: 80417",
"For technical questions, contact: emittertechsupport@vishay.com", disclaimer
line referencing www.vishay.com/doc?91000.
