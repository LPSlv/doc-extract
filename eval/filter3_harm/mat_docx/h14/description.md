Page header (outside the drawing frame): "EXAMPLE BOARD LAYOUT" / "PW0014A" / "TSSOP - 1.2 mm max height" / "SMALL OUTLINE PACKAGE".

All drawings sit inside one ruled rectangular frame. Drawing-office number at the bottom right of the frame: "4220202/B  12/2023".

## Figure 1 — Land pattern example (PCB footprint plan view)

**Type:** mechanical land-pattern drawing, top view, two vertical columns of rectangular solder lands (drawn as outlined rectangles, wider than tall), 7 lands per column, 14 total.

Captions beneath the pattern:

- "LAND PATTERN EXAMPLE"
- "EXPOSED METAL SHOWN"
- "SCALE: 10X"

Pin numbering and geometry:

- Left column, top land labelled **1**, bottom land labelled **7** (numbering runs top to bottom).
- Right column, top land labelled **14**, bottom land labelled **8** (numbering runs bottom to top, i.e. counter-clockwise package numbering).
- A vertical dash-dot centreline through the middle of the pattern is labelled **SYMM** (with the centreline symbol ℄ beneath it).
- A horizontal dash-dot centreline at mid-height, on the right, is also labelled **SYMM** ℄.

Dimension callouts (all values in parentheses, i.e. reference dimensions, in mm):

| Callout | What it dimensions |
|---|---|
| 14X (1.5) | Land length (horizontal width of each land), 14 places |
| 14X (0.45) | Land width (vertical height of each land), 14 places |
| 12X (0.65) | Land pitch, centre-to-centre between adjacent lands, 12 places |
| (5.8) | Overall span between the left and right column centrelines |
| (R0.05) TYP | Corner radius of the land, typical — leader line points to the top-right land |

## Figure 2 — Solder mask details (two paired detail views)

**Type:** two side-by-side enlarged detail views of a single land, each an outlined rounded rectangle with an inner offset outline, under the shared caption "SOLDER MASK DETAILS".

**Left detail — non-solder-mask-defined.** Callouts with leader lines: "SOLDER MASK OPENING", "METAL", "EXPOSED METAL". Dimension below the view: "0.05 MAX / ALL AROUND" (gap between the metal edge and the mask opening). Sub-caption: "NON-SOLDER MASK DEFINED / (PREFERRED)".

**Right detail — solder-mask-defined.** Callouts with leader lines: "METAL UNDER SOLDER MASK", "SOLDER MASK OPENING", "EXPOSED METAL". Dimension below the view: "0.05 MIN / ALL AROUND". Sub-caption: "SOLDER MASK DEFINED".

Notes printed below the frame (continuation numbering): "NOTES: (continued)"; "6. Publication IPC-7351 may have alternate designs."; "7. Solder mask tolerances between and around signal pads can vary based on board fabrication site."

Page furniture: Texas Instruments logo and "www.ti.com" at the foot.
