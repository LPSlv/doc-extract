**Figure (commutative diagram, p32).** The page's only vector artwork: an unnumbered,
uncaptioned commutative diagram set as a display, introduced by "The relation can be
placed in the diagram" and followed by "where the diagonal map is the deterministic
density dependent probability flow."

Layout — a 2x2 square with one extra node hanging below the bottom-right corner:

```
        X_0  ──── Φ^ω_{0,t} ────▶  X_t^ω
         │                          │
       Law│                         │Law and E
         ▼                          ▼
        ρ_0  ──── P*_{0,t} ─────▶   ρ_t
           ╲                        ‖
            ╲  (Φ_{0,t})_#          ‖
             ╲                      ‖
              ─────────────────▶   ρ_t
```

Nodes and edges in full:

- Top-left node $X_0$; top-right node $X_t^{\omega}$. Horizontal arrow left to right,
  labelled above with $\Phi^{\omega}_{0,t}$ (the surrounding text writes the same map
  with general start time as $\Phi^{\omega}_{s,t}$).
- Left vertical arrow, $X_0 \to \rho_0$, labelled "Law" on its left.
- Right vertical arrow, $X_t^{\omega} \to \rho_t$, labelled "Law and **E**" on its
  right (bold **E** = expectation).
- Middle-row horizontal arrow $\rho_0 \to \rho_t$, labelled below with
  $\mathsf{P}^{*}_{0,t}$ — the dual Markov semigroup of equation (12.7).
- Diagonal arrow from $\rho_0$ down and to the right to a third node $\rho_t$ placed
  below the middle-row $\rho_t$; labelled $(\Phi_{0,t})_{\#}$ (pushforward) to the
  left of the arrow. This is the "diagonal map" the following sentence calls the
  deterministic density dependent probability flow.
- A double vertical rule (identification / equality, not an arrow) joining the
  middle-row $\rho_t$ to the bottom $\rho_t$.

The diagram therefore asserts that the law of the random-flow image
$(\Phi^{\omega}_{0,t})_{\#}\rho_0$ averaged over noise, the dual semigroup
$\mathsf{P}^{*}_{0,t}\rho_0$, and the deterministic pushforward
$(\Phi_{0,t})_{\#}\rho_0$ all give the same marginal $\rho_t$.

No chart, table or other figure on the page. The remaining content is running text
and displayed equations, including formula (12.7) and Theorem 12.3, which are part of
the extractable text layer.
