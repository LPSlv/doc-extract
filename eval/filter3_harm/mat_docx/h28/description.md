Three line graphs on this page, arranged as two side by side over one centred
below, all inside a light-blue ruled frame.

## Fig 9. Gate-source threshold voltage as a function of junction temperature.

Type: line graph, two curves, linear axes.
- X: T_j (°C), linear, ticks at −60, 0, 60, 120, 180 (gridlines every 30 °C —
  six vertical gridlines across the plot). Axis caption "T_j (°C)".
- Y: V_GS(th) (V), linear, 0 to 1, labelled ticks every 0.2 (0, 0.2, 0.4, 0.6,
  0.8, 1), gridlines at each labelled tick plus midpoints (every 0.1).
- Plot-ID string in the top-right corner of the plot: `03aj65`.

Two curves, both falling monotonically with temperature:
- **typ** — dashed, labelled inline "typ" near the middle of the plot. Starts at
  ~0.84 V at −60 °C (read from axis), ~0.70 V at ~+30 °C, ends ~0.40 V at
  ~+150 °C (read from axis). It does not extend to the 180 °C tick.
- **min** — solid, labelled inline "min". Starts ~0.54 V at −60 °C, ~0.47 V at
  0 °C, ~0.40 V at +60 °C, ends ~0.26 V at ~+150 °C (all read from axis).

Condition line below the plot: "I_D = 1 mA; V_DS = V_GS".
Caption (blue, bold): "Fig 9. Gate-source threshold voltage as a function of
junction temperature."

## Fig 10. Sub-threshold drain current as a function of gate-source voltage.

Type: line graph, two curves, log Y / linear X.
- X: V_GS (V), linear, 0 to 1, labelled ticks at 0, 0.2, 0.4, 0.6, 0.8 and 1
  (the final "1" is printed as a superscript-looking glyph immediately after the
  axis caption). Axis caption "V_GS (V)".
- Y: I_D (A), logarithmic, decades from 10⁻⁶ to 10⁻³ (labelled 10⁻⁶, 10⁻⁵, 10⁻⁴,
  10⁻³), with full log minor gridlines inside each decade.
- Plot-ID string top right of the plot: `03aj64`.

Two straight (on log-linear axes) rising curves, labelled inline:
- **min** — solid line, leftmost. Crosses 10⁻⁶ A at ~0.12 V, 10⁻⁵ A at ~0.19 V,
  10⁻⁴ A at ~0.31 V, reaches 10⁻³ A at ~0.44 V (all read from axis).
- **typ** — dashed line, to the right. Crosses 10⁻⁶ A at ~0.37 V, 10⁻⁵ A at
  ~0.44 V, 10⁻⁴ A at ~0.55 V, reaches 10⁻³ A at ~0.69 V (read from axis).

Condition line below the plot: "T_j = 25 °C; V_DS = 5 V".
Caption (blue, bold): "Fig 10. Sub-threshold drain current as a function of
gate-source voltage."

## Fig 11. Input, output and reverse transfer capacitances as a function of drain-source voltage; typical values.

Type: line graph, three curves, log-log.
- X: V_DS (V), logarithmic, 10⁻¹ to 10², labelled 10⁻¹, 1, 10, 10². Axis caption
  "V_DS (V)".
- Y: C (pF), logarithmic, 10 to 10³, labelled 10, 10², 10³. Full log minor
  gridlines in both directions.
- Plot-ID string top right of the plot: `03am38`.

Three falling curves, each labelled at its right-hand end:
- **C_iss** — topmost. Starts at ~1000 pF at V_DS = 0.1 V, ~600 pF at ~0.7 V,
  ~500 pF at 10 V, ends ~450 pF at ~20 V (read from axis). Flattest of the three.
- **C_oss** — middle. Starts ~600 pF at 0.1 V, ~250 pF at 1 V, ends ~100 pF at
  ~20 V (read from axis).
- **C_rss** — bottom. Starts ~500 pF at 0.1 V, ~200 pF at 1 V, ends ~65 pF at
  ~20 V (read from axis).

All three curves stop at roughly V_DS = 20 V; none is drawn out to the 10² decade.

Condition line below the plot: "V_GS = 0 V; f = 1 MHz".
Caption (blue, bold): "Fig 11. Input, output and reverse transfer capacitances as
a function of drain-source voltage; typical values."

Page furniture (not part of the figures): running head "Philips Semiconductors"
left, "PMV30UN" right, sub-head "µTrenchMOS™ ultra low level FET"; footer
"9397 750 11569", "© Koninklijke Philips Electronics N.V. 2003. All rights
reserved.", "Product data", "Rev. 01 — 25 June 2003", "7 of 12".
