**Navigation icon strip (top of page).** Four small TI datasheet icons with
labels: `Ordering & quality`, `Technical documentation`, `Design & development`,
`Support & training`. Below them the Texas Instruments logo, device `SN74LV1T34`,
document `SCLS743E – DECEMBER 2013 – REVISED FEBRUARY 2024`.

**Figure (block diagram with waveform insets, lower half of page; captioned
`Switching Thresholds for 1.8-V to 3.3-V Translation`).**

The drawing has three level-shifter signal chains plus two waveform sketches.

Top-left chain (small): box `5.0V 3.3V System` — hollow block arrow → box
`LV1Txx Logic` — hollow block arrow → box `5.0V System`. Annotation above the
input arrow: `VIH = 2.0V` / `VIL = 0.8V`. Boxed supply label above the logic
block: `Vcc = 5.0V`.

Top-right chain (small): box `5.0V, 3.3V 2.5V, 1.8V 1.5V, 1.2V System` — block
arrow → box `LV1Txx Logic` — block arrow → box `1.8V System`. Annotation above
the input arrow: `VIH = 0.99V` / `VIL = 0.55V`. Boxed supply label above the
logic block: `Vcc = 1.8V`.

Centre chain (large, the subject of the caption): boxed supply label
`Vcc = 3.3V` connected by a vertical line down to the top of a large box
`LV1Txx Logic`. To its left, box `5.0V, 3.3V 2.5V, 1.8V System` feeds it through
a wide hollow block arrow; to its right a wide hollow block arrow feeds box
`3.3V System`.

Two large hollow downward block arrows drop from the input side and the output
side of the centre chain to two digital waveform sketches:

- Left waveform (input side): a trapezoidal pulse with sloping edges. A dashed
  level on the rising edge is labelled `VIH min = 1.36V`; a dashed level on the
  falling edge is labelled `VIL min = 0.8V`.
- Right waveform (output side): a similar trapezoidal pulse with steeper edges.
  A dashed level near the top is labelled `VOH min = 2.4V`; a dashed level near
  the bottom is labelled `VOL max = 0.4V`.

Caption under the whole drawing: `Switching Thresholds for 1.8-V to 3.3-V
Translation`.

The Package Information table, features list and description on this page are
ordinary extractable text and are not reproduced here.
